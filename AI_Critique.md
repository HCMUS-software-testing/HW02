# AI Critique

Trong bài tập này, em sử dụng Gemini 3.1 Pro cùng các Agent Skills tự xây dựng để áp dụng kỹ thuật Phân vùng tương đương (EP) và Phân tích giá trị biên (BVA) cho 4 tính năng. AI giúp tạo test case nhanh hơn nhưng khi rà soát vẫn cần chỉnh sửa khá nhiều. Quyết định viết skill giúp việc mỗi lần prompt cùng 1 yêu cầu em không cần phải ghi lại giải thích dài nhiều lần. Trong quá trình dùng, AI mắc các lỗi sau: tạo quá nhiều lớp tương đương không cần thiết (đặc biệt ở FR-03), bỏ sót các business rules như kiểm tra họ tên, tên danh mục trùng hoặc ID không tồn tại, áp dụng EP chưa phù hợp cho FR-10 thay vì State Transition Testing, và giả định mọi kiểm thử đều thực hiện qua UI trong khi Amột số trường hợp phải kiểm tra bằng API. Nguyên nhân là AI chỉ phân tích yêu cầu dựa trên văn bản, chưa hiểu đầy đủ cách hệ thống hoạt động nên dễ bỏ qua các ràng buộc và quy tắc ẩn. Qua bài tập, em nhận thấy AI rất hữu ích để tạo bản nháp test case, nhưng không thể thay thế người kiểm thử. Kết quả của AI luôn cần được đối chiếu với hệ thống thực tế để đảm bảo tính chính xác.
