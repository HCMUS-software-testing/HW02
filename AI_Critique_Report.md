
# Đánh giá AI

Trong bài tập này, AI hữu ích khi hỗ trợ phác thảo cấu trúc kiểm thử miền: xác định biến đầu vào/đầu ra, phân hoạch lớp tương đương, chọn giá trị biên và đề xuất test case ban đầu cho các tính năng của EShop. Tuy nhiên, AI cũng tạo ra một số kết quả chưa đúng nên em phải rà soát và chỉnh lại. Vấn đề lớn nhất là AI đôi khi đi lệch khỏi nguyên tắc kiểm thử hộp đen. AI từng gợi ý kiểm tra trực tiếp bảng dữ liệu, bộ đếm đăng nhập sai, lịch sử dùng coupon hoặc trạng thái giao dịch bên trong hệ thống, trong khi bài chỉ yêu cầu quan sát qua UI, API public, mã HTTP và phản hồi hiển thị được. AI cũng có lúc nhầm biến trạng thái hệ thống với dữ liệu nhập trực tiếp. Ví dụ, số lần đăng nhập sai liên tiếp, trạng thái coupon đã dùng, sản phẩm đã có trong giỏ, hoặc vai trò admin phải được mô hình hóa thành điều kiện tiền đề, không phải field người kiểm thử nhập vào. Ngoài ra, AI dễ tập trung vào validation thông thường nên bỏ sót các kịch bản bảo mật như race condition, token JWT cũ, giả mạo `user_id`, checkout bypass hoặc gọi API trực tiếp để vượt ràng buộc giao diện. Nguyên nhân là AI chủ yếu suy luận từ đặc tả tĩnh và mẫu kiểm thử phổ biến, không thật sự chạy hệ thống. Bài học em rút ra là AI chỉ nên là trợ lý gợi ý; mọi test case phải được đối chiếu với đặc tả, thực thi trên SUT thật và có bằng chứng trước khi đưa vào báo cáo.
